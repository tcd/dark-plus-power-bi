{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/tcd/dark-plus-power-bi/master/schema/power-bi-report-theme.schema.json",
  "title": "Power BI Report Theme",
  "description": "JSON schema for Power BI Report Theme JSON files.",
  "type": "object",
  "required": [
    "name"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "theme name"
    },
    "dataColors": {
      "description": "The list of color hexadecimal codes to use to color shapes that represent data in Power BI Desktop visuals. This list can have as many or as few colors as you want. Once all colors from this list have been used, if the visual still needs more colors, it reverts back to using Power BI's default color palette.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}